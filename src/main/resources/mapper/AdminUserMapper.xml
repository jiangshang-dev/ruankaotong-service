<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heima.mapper.AdminUserMapper">

    <select id="selectByAccount" resultType="com.heima.entity.AdminUser">
        SELECT id, account, password, name, created_at, updated_at
        FROM rk_admin
        WHERE account = #{account}
        LIMIT 1
    </select>

    <select id="selectById" resultType="com.heima.entity.AdminUser">
        SELECT id, account, password, name, created_at, updated_at
        FROM rk_admin
        WHERE id = #{id}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rk_admin (account, password, name)
        VALUES (#{account}, #{password}, #{name})
    </insert>

    <update id="updatePassword">
        UPDATE rk_admin
        SET password = #{password}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM rk_admin
    </select>
</mapper>
